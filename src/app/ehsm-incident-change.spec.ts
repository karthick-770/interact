import { EhsmIncidentChange } from './ehsm-incident-change';

describe('EhsmIncidentChange', () => {
  it('should create an instance', () => {
    expect(new EhsmIncidentChange()).toBeTruthy();
  });
});
